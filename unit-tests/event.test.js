const { isAuthenticated, isAdmin } = require('../middleware/authMiddleware');

describe('Auth Middleware', () => {
  test('isAuthenticated should return true if user is authenticated', () => {
  });

  test('isAdmin should return true if user is an admin', () => {
  });
});